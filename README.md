# ReRoom
ReRoom is a project to re make the server of rec room but with a dns server private

# DNS
The project will used a server dns for re make the server and retro enginiring the game to know the game protocol will be used a version of 2026 or a version of 2025 to help other for re make the server in a recent version 
for the moment will use rec place for a version of 2023
